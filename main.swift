import Foundation

struct Main {

    // MARK: - Private Property(ies).

    private let modules: [Module]

    // MARK: - Constructor(s).

    init(modules: [Module] = []) {
        self.modules = modules
    }
}